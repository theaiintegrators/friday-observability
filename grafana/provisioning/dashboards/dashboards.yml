apiVersion: 1
providers:
  - name: 'default'
    orgId: 1
    folder: 'Friday'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards # This path will be mounted inside the container